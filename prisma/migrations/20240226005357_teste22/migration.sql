BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Credits] ADD CONSTRAINT Credits_pkey PRIMARY KEY CLUSTERED ([ID]);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
